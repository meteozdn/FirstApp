name: engquiz
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.2 <3.0.0'


dependencies:
  firebase_auth: ^4.4.2
  google_sign_in: ^6.0.2
  flutter:
    sdk: flutter
  cloud_firestore: ^4.4.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/images/
    - lib/images/avatars/